version: '3'

services:
  api:
    build:
      dockerfile: Dockerfile.dev
      context: ./api
    command: npm run test --watch

  nginx:
    volumes:
      - ./nginx/nginx.conf.dev:/etc/nginx/conf.d/nginx.conf
